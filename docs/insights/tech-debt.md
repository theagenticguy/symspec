# symspec · Tech debt

Grounded scan for real rot signals: `TODO`/`HACK`/`FIXME`/`XXX`, `@ts-expect-error`, `as unknown as`, `eslint-disable`/`biome-ignore`, `@deprecated`, version pins, tool carve-outs, and disclosed scope limitations. Every row cites `path:LINE`. Where a category is genuinely clean, it is stated as clean rather than padded.

## Clean categories (verified, not padded)

- **No `TODO`/`HACK`/`FIXME`/`XXX` markers** anywhere in `src/`, `adversarial/`, or `scripts/` — grep returns zero hits. The only `placeholder`/`for now` strings are the disclosed certify placeholder (`src/cli/index.ts:688`, `:1231`, `:1238`) and GtWR heuristic notes (`src/lint/gtwr.ts:412`, `:418`).
- **No `@ts-expect-error` / `@ts-ignore`** in the codebase, and **no `biome-ignore`** suppression comments. The only `eslint-disable` comments are 8 `no-console` disables in the v3.4 adversarial harness's console-reporting `main` (`adversarial/harness.ts:85`, `:126`–`:152`) — a CLI diagnostic tool that legitimately prints to stdout; none appear in `src/`.
- **No `@deprecated` APIs** in `src/` — the only "deprecated" strings are test fixtures (`src/parse/__tests__/negation.test.ts:59`).
- **Exactly one `as unknown as` cast** in the whole tree (`src/cli/dense.ts:121`) — narrow and documented, not a pattern.

The rot that exists is concentrated in **one honestly-disclosed semantic gap** (certify placeholder theorems) and a set of **calibration / config / heuristic** shortcuts. The v3.0–v3.4 tiers landed clean, and so did the issue-#2 adversarial hardening: the new `src/formal/quantity-alias.ts`, `src/formal/relational.ts`, and `src/cli/field.ts` add no `TODO`/`HACK`/`FIXME` markers, no suppression comments, and no casts. The suppression-comment and cast surface is close to pristine.

## Register (most impactful first)

| # | Item | Location | Type | Cost-of-removal |
|---|------|----------|------|-----------------|
| 1 | `certify` maps every requirement to a placeholder `True`/`by decide` theorem — the Lean certificate attests only that the toolchain **ran and the file elaborates**, not that the spec is consistent | `docToTheorems` `src/cli/index.ts:1236-1249`; disclosure msg `src/cli/index.ts:682-692`; spec `.erpaval/specs/001-symspec-v2/followups.md:3-19` | known-limitation | **high** — needs a real EARS→Lean encoding (`guard → obligation` as a Lean `Prop`, reusing the atomize/encode model that feeds SMT). followups.md states no published EARS→Lean pipeline exists; this is genuine research effort, deliberately reserved for v3+. |
| 2 | Two DIFFERENT, both-calibrated cosine thresholds, each an uncalibrated-against-a-labeled-set constant: the paraphrase tier `DEFAULT_SEMANTIC_THRESHOLD = 0.72` (favors recall — a miss hides a provable conflict) and the graph tier `DEFAULT_GRAPH_THRESHOLD = 0.82` (favors precision — whole-sentence relatedness). Both were MEASURED against the real BGE band (divergent paraphrases ~0.75–0.79, unrelated same-domain ~0.44–0.58), not tuned on a labeled corpus | `src/formal/semantic.ts:110` (default), `:91-110` (doc), `:140` (`threshold` resolve); graph `src/formal/graph.ts:114`, `:85-112` (why not the same value) | shortcut (uncalibrated constant) | **low–med** — the values are measured and split by purpose (an earlier docs entry wrongly said semantic was 0.82 — that was the pre-retune value, corrected in the 2026-07-12 eval-fix session). Both overridable via `--semantic-threshold`, both consumers suggestion-only (`info`/propose), so a miss is a lost hint, never a wrong verdict. Remaining debt is "no labeled-set precision/recall study." |
| 3 | `pnpm-workspace.yaml` ships literal placeholder strings `set this to true or false` for three native builds under `allowBuilds:` — unresolved config left in a committed file | `pnpm-workspace.yaml:4-6` (`onnxruntime-node`, `protobufjs`, `sharp`) | shortcut (unfinished config) | **low** — decide each build flag once. The real build gating lives in `package.json:76-83` (`onlyBuiltDependencies` allows esbuild/lefthook/z3-solver; `ignoredBuiltDependencies` pins `protobufjs`), so the workspace-file placeholders are stale/duplicative rather than load-bearing. |
| 4 | GtWR lint rules are admittedly low-precision heuristics: e.g. R5 "For now, flag all but provide low confidence" (`src/lint/gtwr.ts:418`), R33 "bare number without a range/tolerance marker" (`:866`). 7 `Heuristic`/`For now` markers remain in the file | `src/lint/gtwr.ts:412`, `:418`, `:596`, `:618`, `:806`, `:866`, `:969` | shortcut (heuristic) | **med** — replacing regex heuristics with real POS/dependency parsing is nontrivial; findings are `warn`/`info` (non-blocking per `src/pipeline/gate.ts:98` `blockingFindings`), so precision misses cost noise, not gate failures. The issue-#2 R6 rewrite REDUCED this debt: R6's "Simplified: look for bare numerics" heuristic is gone, replaced by named unit tables (`R6_RECOGNIZED_UNITS`/`R6_MULTIWORD_UNITS`/`R6_SYMBOL_UNITS`, `src/lint/gtwr.ts:116`, `:212`, `:224`) covering mass/volume/rate/distance/currency/calendar plus a `[0,1]` ratio escape. |
| 5 | The v3 numeric label extractor is a shallow heuristic: it takes the last ≤3 alphabetic words before the comparator as the quantity label, trimming trailing fillers. An over- or under-captured label splits one quantity into two keys, and the two bounds never meet — a silent numeric false negative | `src/formal/numeric.ts:186` (`labelBefore`), `:160` (`quantityKey`) | shortcut (heuristic) | **med** — robust quantity identity needs NP-chunking, not a word-window. Fails in the honest direction (a miss, never a fabricated conflict — `src/formal/numeric.ts:21`); the verdict it does emit is sound (LIA/LRA is decidable). The issue-#2 hardening mitigated (not removed) this: a same-quantity-two-verbs split is now surfaced as a propose-only `FND_QUANTITY_ALIAS_CANDIDATE` that demotes `verified` and suggests the discharging `glossary add`, so the miss is loud and actionable rather than silent (`src/formal/quantity-alias.ts:160`). Deliberately NOT closed by loosening the key — stripping comparator words from the key fabricated a false verdict, so the key stays conservative (`src/formal/numeric.ts:211`). |
| 6 | Two finding producers don't emit their `FND_*` code as a string literal — they discriminate on other fields (`kind`, a `certified` boolean) and are bridged by mapping functions | `src/formal/codes.ts` (`structuralKindToFndCode` :279, `solverKindToFndCode` :293, `certifiedToFndCode` :298) | shortcut (indirection) | **low** — the AC-6-3 reachability test already guards it and the bridge maps are single-sourced; cost is only the conceptual overhead of the extra indirection layer. |
| 7 | `GateInput.parseFailed` is optional because the parse-tier→gate wiring (`ParseResult`, AC-2-8) was never built; gate falls back to surface-check-only when the flag is omitted, and no caller currently wires it | `src/pipeline/gate.ts:58-69` (field), `:164-172` (`gateRequirements` never sets it) | known-limitation (unbuilt path) | **med** — requires threading real parse outcomes from Tier 1/2/3 into the gate; until then parse-failure provenance never reaches the gate. Note the gate itself is no longer purely surface-check-only — the issue-#2 waiver-awareness added a `waivers` param that re-admits a requirement whose only blocking finding is waived (`src/pipeline/gate.ts:120-133`) — but the `parseFailed` provenance path remains unwired. |
| 8 | `as unknown as` cast to read Zod's internal `.def` view off a schema — reaches into library internals that can shift across Zod majors | `src/cli/dense.ts:121` (`defOf`), interface `:110-117` | hack (library-internals reach) | **low** — isolated to one helper; the pinned Zod (`^4.4.3`, `package.json:73`) holds the shape today, but a Zod major bump could silently change the internal `.def` view. |
| 9 | `tokenizer.encode(...) as Encoding` cast at the ONNX/tokenizer boundary — the `@huggingface/tokenizers` result is narrowed to a locally-declared `Encoding` interface rather than the library's own type | `src/formal/embed.ts` (`Encoding` interface `:89`, cast `:136`) | shortcut (boundary cast) | **low** — thin, deliberate projection of the fields consumed; no runtime risk, just an untyped seam at a lazy-loaded dependency. |
| 10 | knip carve-outs: `wink-nlp` + `wink-eng-lite-web-model` force-ignored because knip can't trace `await import(VARIABLE)` variable-specifier lazy loads; `diff` binary force-listed | `knip.json:6` (`ignoreDependencies`), `:7` (`ignoreBinaries`); lesson `.erpaval/solutions/conventions/knip-variable-specifier-dynamic-import.md` | known-limitation (tooling) | **low** — the carve-out is the sanctioned fix (a literal import would re-couple the model to the hot path). Risk: a future cleanup deletes the ignore, then the "unused" deps. |
| 11 | Three honest scope limits remain **by design**, all disclosed in-tool: (a) the temporal tier is **bounded / sound-for-UNSAT, not complete-for-SAT** — a `sat`-at-`k` result is never a consistency certificate; (b) **pragmatic/contextual ambiguity** is still not decided — now surfaced as a structured `FND_AMBIGUITY_NEEDS_JUDGMENT` hand-off; (c) **aggregate/conservation, cross-quantity arithmetic, and emergent-structural (odd-cycle/pigeonhole) conflicts are NOT attempted** — inferring the finite resource or relation from prose would require guessing, which violates sound-modulo-atomization | temporal `src/formal/temporal.ts:13-25`; ambiguity punt `src/formal/ambiguity.ts:401`; relational `src/formal/relational.ts:22-37`; README `README.md:252-257` | known-limitation (by design) | **high** — (a) unbounded temporal completeness needs a loopback-lasso encoding; (b) contextual ambiguity is genuinely undecidable mechanically; (c) is a deliberate refusal, not missing work — the boundary sits exactly at "detect the shape and demote (`FND_RELATIONAL_UNCHECKED`)" vs "fabricate a solver." Building a relational/aggregate solver would be the false-positive risk the doctrine forbids. Not accidental debt; listed for completeness. See the correction note below. |

> **Correction vs the pre-v3 register.** The v2-era entry "no temporal/ordering logic and no numeric/arithmetic reasoning" is **stale**: v3.0 added the deterministic numeric/arithmetic tier (default on), v3.3 added the bounded temporal tier (opt-in), and the issue-#2 hardening added the propose-only quantity-alias and detect-and-demote relational tiers. The README's rewritten scope prose is accurate: it states the formal stage is **sound modulo atomization** (`README.md:376`), that the numeric tier reasons about arithmetic (`README.md:292`), and that what no sound extractor can recover from prose — aggregate/conservation, cross-quantity arithmetic, emergent structural impossibility — is flagged (`FND_RELATIONAL_UNCHECKED`) and demotes `verified` rather than silently passing (`README.md:238-239`, `:252-257`). What survives as real by-design scope is item #11 above.

## The biggest one: certify's placeholder theorems

The `certify` command runs the full Lean 4 toolchain — batched theorem emission, elaboration, `#print axioms` provenance capture, artifact retention — but `docToTheorems` maps **every requirement to a trivial `True`/`by decide` proposition keyed by its sanitized UUID** (`src/cli/index.ts:1236-1249`). The code comment states it plainly: this "exercises the full Lean toolchain path ... but does NOT encode requirement semantics."

The consequence is that a green `FND_CERTIFIED` result attests only that Lean ran and the emitted file elaborates — it is a tautology, not a proof about the spec. The finding message discloses this to the caller in the same breath (`src/cli/index.ts:682-692`): the certificate "attests only that the Lean toolchain ran and the file elaborates — it does NOT yet encode requirement semantics ... the SMT `check` tier is the load-bearing conflict detector today." This is disclosed honestly rather than hidden, which is why it ranks as a known-limitation and not a trap.

The deferral is recorded in `.erpaval/specs/001-symspec-v2/followups.md:3-19`. The v3+ follow-up is to encode EARS requirements as real Lean `Prop`s — reusing the atomize/encode formal model that already feeds the SMT tier, translating the guarded implication `guard → obligation` into a Lean proposition — so the certificate becomes a kernel-checked proof **about the spec's consistency**. followups.md is explicit that "no published EARS→Lean pipeline exists — this is the genuine research contribution the Lean tier was chosen for," and reserves it for after the core tiers stabilize. Cost-of-removal is therefore **high**: it is research effort, not a refactor. The contracts the Lean tier does hold today (batched single file, `#print axioms` provenance, `ERR_LEAN_TOOLCHAIN_MISSING`) are real and hold regardless of the proposition body (`src/cli/index.ts:1232-1234`) — the toolchain scaffolding is done; only the semantic encoding is deferred. Note the issue-#2 adversarial hardening did NOT touch certify: the numeric/quantity-alias/relational work that closed the numeric tier's blind spots landed on the SMT `check` path, leaving the EARS→Lean encoding as the single largest remaining semantic gap.

## See also

- [Module map](../architecture/module-map.md) — 11 shared source citations
- [Processes](../behavior/processes.md) — 9 shared source citations
- [Business logic](business-logic.md) — 9 shared source citations
- [Contract map](contract-map.md) — 8 shared source citations
- [Debugging guide](debugging-guide.md) — 7 shared source citations
