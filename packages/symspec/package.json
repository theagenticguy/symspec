{
  "name": "symspec",
  "version": "1.0.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "symspec v5 greenfield: one operations table projected into a CLI, a machine-readable manifest, and help — built on Effect v4.",
  "license": "Apache-2.0",
  "bin": {
    "symspec": "./bin/symspec.mjs"
  },
  "files": [
    "dist",
    "bin",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "cli": "node dist/cli.mjs",
    "bench": "./scripts/bench.sh",
    "check": "biome ci . && tsc --noEmit && pnpm run build && vitest run"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.102",
    "@huggingface/tokenizers": "^0.1.3",
    "effect": "4.0.0-beta.102",
    "onnxruntime-web": "^1.27.0",
    "wink-eng-lite-web-model": "^1.8.1",
    "wink-nlp": "^2.4.0",
    "z3-solver": "^5.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^25.7.0",
    "tsdown": "^0.22.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "z3-solver"
    ]
  }
}
